specVersion: "v1"
serviceId: "ENCO"
location: "europe-west1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "enco-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
